pipeline {
    agent any

    stages {
        stage('Pull from GitHub') {
            steps {
                git 'https://github.com/baraaAlmasri/Final-Unix-Project'
            }
        }

        stage('Deploy to Server') {
            steps {
                sh './ci-cd/deploy.sh'
            }
        }
    }
}
